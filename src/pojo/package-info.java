/**
 * plain old java object son clases simples sin dependencias de otros frameworks
 * tienen atributos privados para mantener la encapsulacion y a estos atributos
 * se acceden a traves de getters y setters publicos, es obligatorio que tengan
 * al menos un constructor
 */
package pojo;
